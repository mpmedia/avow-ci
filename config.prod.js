// Production configuration overrides for DozerJS
var config = {

  // Environment configuration
  env: {
    // Public root
    publicHTTP: 'public/dist',
    // Port to run over
    port: 8282
  }
};

module.exports = config;
